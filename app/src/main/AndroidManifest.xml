<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ondemandhomerepairservices.on_demandhomerepairservices">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterServiceProviderActivity" />
        <activity android:name=".LoginHomeOwner" />
        <activity android:name=".LoginAdmin" />
        <activity android:name=".LoginServiceProvider" />
        <activity android:name=".RegisterSuccess" />
        <activity android:name=".AdminUserListActivity" />
        <activity android:name=".AdminServiceListActivity" />
        <activity android:name=".RegisterAdminActivity" />
        <activity android:name=".RegisterHomeOwnerActivity" />
        <activity android:name=".RegisterMainActivity" />
        <activity android:name=".LoginCheckSPActivity" />
        <activity android:name=".LoginCheckAdminActivity" />
        <activity android:name=".ServiceProviderAvailableTime" />
        <activity android:name=".Main2Activity"/>
        <activity android:name=".ServiceProviderAddNewService"/>
        <activity android:name=".ServiceProviderServiceProvided"/>
        <activity android:name=".LoginCheckHOActivity"></activity>
    </application>

</manifest>